apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nginx-pdb
spec:
  minAvailable: 2  # Ensures at least 2 pods stay running during voluntary disruptions
  selector:
    matchLabels:
      app: nginx
